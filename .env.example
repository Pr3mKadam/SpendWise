# Copy to `.env.local` and fill in. Never commit `.env.local`.

# Optional: Gemini for natural-language transaction parsing
# VITE_GEMINI_API_KEY=

# Supabase — https://supabase.com (leave empty to run without login / cloud sync)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
