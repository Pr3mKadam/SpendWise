# Architecture

> Auto-generated by /map on 2026-04-26

## Overview

SpendWise is a React-based single-page application (SPA) focused on personal finance and expense management. It provides real-time banking synchronization, budgeting tools, shared wallets, AI coaching (via Gemini), and parental controls.

```
┌─────────────────────────────────────────┐
│              [Entry Point]              │
│          src/main.tsx -> App.tsx        │
├─────────────────────────────────────────┤
│         [Business Logic Layer]          │
│       src/contexts/ & src/hooks/        │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│      src/lib/ & src/services/           │
└─────────────────────────────────────────┘
```

## Components

### UI Components
- **Purpose:** Provide the user interface, including dashboards, charts, and modals.
- **Location:** `src/components/`
- **Dependencies:** React, Recharts, Lucide React, Tailwind CSS

### Contexts
- **Purpose:** Global state management for authentication and parental controls.
- **Location:** `src/contexts/`
- **Dependencies:** React Context API, Supabase Client

### Services & Libraries
- **Purpose:** API wrappers, database interactions, and AI integrations.
- **Location:** `src/services/` and `src/lib/`
- **Dependencies:** `@supabase/supabase-js`, native `fetch` (for Gemini and Razorpay)

## Data Flow

1. User interacts with a view in `src/components/` (e.g., `BankSyncView.tsx` or `AICoach.tsx`).
2. Component reads/updates state via React Contexts (`AuthContext`, `ParentalControlContext`).
3. For persistent actions, the component calls functions in `src/lib/supabaseData.ts` or `src/lib/sharedWalletsApi.ts`.
4. The service layers communicate with the Supabase PostgreSQL backend or external APIs (Razorpay, Gemini).

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Supabase | Database / Auth | Main backend for user accounts, transactions, and shared wallets |
| Razorpay | API | Banking and transaction synchronization (`src/utils/razorpaySync.ts`) |
| Gemini AI | API | Financial AI Coaching and natural language inputs (`src/services/ai.ts`) |

## Technical Debt

- [ ] No `TODO` or `FIXME` markers found, but ensure that direct `fetch` calls to APIs like Gemini/Razorpay are securely managing API keys (avoid exposing secrets in the frontend).

## Conventions

**Naming:** PascalCase for React components, camelCase for services and utilities.
**Structure:** Feature-based flat component structure, centralized API wrappers in `lib` and `services`.
**Testing:** No automated testing framework or test files detected in the current structure.
