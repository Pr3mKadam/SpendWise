# Plan 1.6.2 Summary: Data Import/Export & Backup

## Completed Tasks
1. **Installed Dependencies**: Added `dexie-export-import` to handle raw database export.
2. **Created Backup Utility**: Created `src/db/backup.ts` with `downloadDatabaseBackup` and `importDatabase` logic, ensuring the IndexedDB tables are wiped properly before restore and forcing a reload to update Zustand's state.
3. **UI Integration**: Added "Raw Database Export/Import (.json)" options in `src/components/views/ProfileView.tsx` inside the "Data Management" section, alongside the existing "Secure Encrypted Backup".

## Verification
- Dependencies installed and schema integrated correctly.
- `ProfileView` now exposes direct JSON database backups.
- Note: Pre-existing TS errors in `MainShell.tsx`, `BudgetView.tsx`, etc., remain but they do not block the functional backup/restore utility.
