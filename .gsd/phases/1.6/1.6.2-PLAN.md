---
phase: 1.6
plan: 2
wave: 1
---

# Plan 1.6.2: Implement Data Import/Export & Backup

## Objective
Provide a robust data portability solution by implementing JSON export and import functionalities for the Dexie.js database.

## Context
- .gsd/ROADMAP.md
- src/db/db.ts
- src/components/features/settings/DataManagement.tsx (or similar settings component)

## Tasks

<task type="auto">
  <name>Implement Dexie Export/Import Logic</name>
  <files>
    - src/db/backup.ts
    - package.json
  </files>
  <action>
    - Install `dexie-export-import`.
    - Create utility functions `exportDatabase()` to generate a Blob/JSON file of the entire Dexie database.
    - Create `importDatabase(file)` to restore the Dexie database from a JSON file, including error handling.
  </action>
  <verify>npm list dexie-export-import && npx tsc --noEmit</verify>
  <done>Export and Import utility functions are implemented and type-checked.</done>
</task>

<task type="auto">
  <name>Build Data Management UI</name>
  <files>
    - src/components/views/ProfileView.tsx (or create new settings view)
  </files>
  <action>
    - Add a "Data & Privacy" section to the user profile/settings view.
    - Add a "Download Backup" button that triggers the export and downloads a JSON file.
    - Add a "Restore Backup" button with file input to upload JSON and restore DB.
    - Add a confirmation dialog before restoring, warning that current data will be overwritten.
  </action>
  <verify>npm run build</verify>
  <done>UI elements for backup and restore exist and trigger the backup functions without errors.</done>
</task>

## Success Criteria
- [ ] Users can download their entire financial data history as a JSON file.
- [ ] Users can successfully restore their database from a previously downloaded JSON file.
