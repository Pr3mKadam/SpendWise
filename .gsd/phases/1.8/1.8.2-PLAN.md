---
phase: 1.8
plan: 2
wave: 1
---

# Plan 1.8.2: Quick-Add Panel & Transaction UX Polish

## Objective
Elevate the transaction entry and history UX. Replace the floating MagicInput with an inline "Quick-Add Panel" that sits in the dashboard as a persistent card, and polish the TransactionList with category color pills, swipe-to-delete visual affordance, and a smarter empty state.

## Context
- `src/components/features/ai/MagicInput.tsx`
- `src/components/features/ai/AIInputTools.tsx`
- `src/components/features/history/TransactionList.tsx`
- `src/components/views/DashboardView.tsx`
- `src/types/index.ts`

## Tasks

<task type="auto">
  <name>Build QuickAddPanel card component</name>
  <files>src/components/features/dashboard/QuickAddPanel.tsx</files>
  <action>
    Create `QuickAddPanel.tsx` — a card that wraps the MagicInput and AIInputTools in a clean panel layout:

    Layout structure:
    - Card header: icon `Zap` + title "Quick Add" + subtitle "Describe, scan or speak your expense"
    - A tab row with 3 tabs: `✏️ Type`, `📷 Scan`, `🎙️ Voice` — each tab activates a different input mode
    - "Type" tab renders the text `<input>` + Sparkles process button (extracted from MagicInput logic)
    - "Scan" tab renders a dashed drop-zone with `<Camera>` icon and hidden `<input type="file" accept="image/*">`
    - "Voice" tab renders a large mic button with pulse animation when listening
    - Below tabs: the prediction confirmation card (`AnimatePresence` motion.div) shared across all modes
    - The component accepts the same `onAdd: (t: Transaction) => void` prop as MagicInput

    Avoid:
    - Do NOT duplicate parser logic — import `processNaturalLanguageExpense` from `utils/parsers/nlp`, `parseVoiceLocally` from `utils/parsers/voice`, `recognizeReceipt`/`parseOfflineReceipt` from `utils/parsers/ocr`
    - Keep the prediction/confirm flow from `MagicInput.tsx` — it already works well, just embed it here
  </action>
  <verify>`npx tsc --noEmit` passes. File exports `QuickAddPanel` default.</verify>
  <done>QuickAddPanel.tsx exists with 3-tab interface (Type/Scan/Voice), prediction card, and correct parser imports.</done>
</task>

<task type="auto">
  <name>Polish TransactionList with pills, empty state, and sort controls</name>
  <files>src/components/features/history/TransactionList.tsx</files>
  <action>
    View the current `TransactionList.tsx` then apply these improvements:

    1. **Category pills**: each transaction row should show a colored badge pill (`badge-pill` CSS class) for the category name, using the color from `useCategories().mergedColors[tx.category]` or fallback `var(--teal)`
    2. **Empty state**: when `transactions.length === 0`, render a centered illustration — a large `Receipt` icon (64px, `var(--text-dim)`), heading "No transactions yet", subtext "Use Quick Add above to log your first expense"
    3. **Sort/filter bar**: above the list, add a small row with:
       - A "Latest" / "Oldest" toggle button (icon `ArrowUpDown`)
       - A category filter dropdown (`<select>`) populated from unique categories in the current transactions, defaulting to "All"
    4. **Row micro-animation**: wrap each row in `motion.div` with `initial={{ opacity:0, x:-8 }}` and `animate={{ opacity:1, x:0 }}` with staggered `transition={{ delay: index * 0.04 }}`

    Avoid:
    - Do NOT refetch data — use the `transactions` prop already passed in
    - Do NOT remove the `onCategoryChange` or `onDelete` prop handlers
  </action>
  <verify>`npx tsc --noEmit` passes. TransactionList renders category pills and empty state without errors.</verify>
  <done>TransactionList has category pills, animated rows, sort bar, and a proper empty state. No TS errors.</done>
</task>

## Success Criteria
- [ ] `QuickAddPanel` replaces `MagicInput` in DashboardView with 3-tab interface
- [ ] `TransactionList` shows category color pills on every row
- [ ] Empty state renders when no transactions exist
- [ ] Sort/filter controls function correctly (client-side only)
