---
phase: 1.7
plan: 2
wave: 1
---

# Plan 1.7.2: Enhance Local OCR & Voice

## Objective
Improve the robustness of offline Tesseract.js OCR and Regex-based voice parsing for better data extraction accuracy.

## Context
- `src/utils/tesseractParser.ts`
- `src/utils/voiceParser.ts`

## Tasks

<task type="auto">
  <name>Enhance Voice Parsing Heuristics</name>
  <files>
    - src/utils/voiceParser.ts
  </files>
  <action>
    - Expand `MERCHANT_CATEGORY_MAP` with more common Indian/global merchants.
    - Improve `extractAmount` regex to handle more edge cases (like "paid 500 for food" or "five hundred").
    - Enhance `extractMerchant` to reliably capture merchant names without picking up stop words.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Voice parser is updated with better regex and broader merchant coverage.</done>
</task>

<task type="auto">
  <name>Improve OCR Parsing Reliability</name>
  <files>
    - src/utils/tesseractParser.ts
  </files>
  <action>
    - Update `parseOfflineReceipt` to better detect the "Total" amount (e.g. checking for "Amount Due", "Grand Total").
    - Improve merchant name extraction from the top of the receipt.
    - Ensure it safely defaults values without crashing if OCR confidence is low.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>OCR parser extracts total and merchant more accurately.</done>
</task>

## Success Criteria
- [ ] OCR handles generic receipts more reliably.
- [ ] Voice parser covers a wider range of conversational inputs.
