---
phase: 1.7
plan: 1
wave: 1
---

# Plan 1.7.1: Purge Gemini & Replace CSV Parsing

## Objective
Remove all dependencies on Google Generative AI (Gemini) to make the application 100% offline-first. Replace AI-based CSV parsing with deterministic logic.

## Context
- `.gsd/ROADMAP.md`
- `src/utils/aiAnalyzer.ts` (To be deleted)
- `src/components/features/sync/CSVImporter.tsx`
- `src/utils/razorpaySync.ts`

## Tasks

<task type="auto">
  <name>Delete aiAnalyzer and GenAI Dependency</name>
  <files>
    - package.json
    - src/utils/aiAnalyzer.ts
  </files>
  <action>
    - Uninstall `@google/generative-ai` from the project.
    - Delete `src/utils/aiAnalyzer.ts` entirely.
    - Note: This will break imports in a few files. We will fix them in the next tasks.
  </action>
  <verify>npm list @google/generative-ai</verify>
  <done>Package is removed and aiAnalyzer file is gone.</done>
</task>

<task type="auto">
  <name>Implement Deterministic CSV Parsing</name>
  <files>
    - src/components/features/sync/CSVImporter.tsx
    - src/utils/razorpaySync.ts
    - src/utils/csvParser.ts (New)
  </files>
  <action>
    - Create `src/utils/csvParser.ts` to parse standard bank CSVs (using simple string splitting or a lightweight parser, mapping known columns to `date, amount, merchant, type`).
    - Use the existing `MERCHANT_CATEGORY_MAP` from `voiceParser.ts` to assign categories to CSV rows.
    - Update `CSVImporter.tsx` to use the new deterministic `parseCSVLocally` function instead of `parseCSVWithAI`.
    - Update `razorpaySync.ts` (if it used Gemini) to use the new local parser.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>CSV parsing works purely locally without API calls.</done>
</task>

## Success Criteria
- [ ] No Gemini API code exists in the codebase.
- [ ] CSV uploads are processed locally.
