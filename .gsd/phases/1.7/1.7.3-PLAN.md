---
phase: 1.7
plan: 3
wave: 2
---

# Plan 1.7.3: Rule-Based Local Advisor

## Objective
Replace the cloud-based AI advisor and personality features with a deterministic, rule-based local financial coach that provides insights based on raw transaction data.

## Context
- `src/components/views/AdvisorView.tsx`
- `src/components/views/ReportsView.tsx`
- `src/components/features/ai/SpendingPersonality.tsx`

## Tasks

<task type="auto">
  <name>Implement Heuristic Financial Advisor</name>
  <files>
    - src/components/views/AdvisorView.tsx
    - src/utils/localAdvisor.ts (New)
  </files>
  <action>
    - Create `src/utils/localAdvisor.ts` with logic to analyze transactions and generate advice (e.g., if Food > 30% of expenses, suggest cooking at home).
    - Expose `getLocalFinancialAdvice(query: string, transactions: Transaction[])` to handle basic keyword matching for queries (like "food", "saving").
    - Refactor `AdvisorView.tsx` to call the local advisor instead of `aiAnalyzer`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Advisor view works using local heuristic functions.</done>
</task>

<task type="auto">
  <name>Implement Heuristic Spending Personality</name>
  <files>
    - src/components/features/ai/SpendingPersonality.tsx
    - src/utils/localAdvisor.ts
  </files>
  <action>
    - Add `getLocalSpendingPersonality(transactions)` to assign an archetype (e.g., "The Foodie" if Food is highest category, "The Minimalist" if few transactions).
    - Refactor `SpendingPersonality.tsx` to use this local function instead of `getSpendingPersonality` from `aiAnalyzer.ts`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Personality assignment is deterministic and local.</done>
</task>

<task type="auto">
  <name>Local Monthly Report Generation</name>
  <files>
    - src/components/views/ReportsView.tsx
    - src/utils/localAdvisor.ts
  </files>
  <action>
    - Add `generateLocalMonthlyReport(month, transactions)` using template strings to build an insightful report without an LLM.
    - Update `ReportsView.tsx` (and any other callers of `generateMonthlyReport`) to use the local function.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Monthly reports generate instantly using local data aggregation.</done>
</task>

## Success Criteria
- [ ] Advisor view responds instantly without API keys.
- [ ] Spending Personality and Reports views function locally.
